<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ixigo.sdk.app"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        tools:ignore="UnusedAttribute"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlightsSDKDemoApp">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FlightsSDKDemoApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MultiModelActivity"></activity>
        <activity android:name=".BusMultiModelActivity"></activity>
        <activity android:name=".TripsFragmentActivity"></activity>
        <activity android:name=".TrainsTripsFragmentActivity"></activity>
        <activity android:name=".FakeLoginActivity"></activity>
        <activity android:name=".FakeDeeplinkActivity"></activity>
    </application>

</manifest>